import java.lang.Math.log10
import java.util.*
import kotlin.math.min

class Solutionasdasd {
    fun solution(numbers: IntArray): IntArray {
        var answer: IntArray = intArrayOf()

        val temp = mutableListOf<Pair<String, Int>>()

        val tte = Array<Int>(10) {0}



        when (numbers) {
            asd -> if (asd) {

            }
            bcd -> asd
        }
        temp.toar
        val testt = mutableListOf<String>(10) {" "}
        var teee = arrayOf<String>()
min()
        teee = testt



        temp.forEachIndexed { index, pair ->  }
        temp.removeA

        temp.sortWith(compareBy { it.second }) { it.second }

        for (i in numbers.indices) {
            for (j in i + 1 until numbers.size) {
                temp.add(numbers[i] + numbers[j])
            }
        }

        val es: Long = 1000000
        log10(es)
        Math.pow(10, 1)
        val te = arrayListOf<String>()
        te.index

        te.sortD
        ('A'..'Z').contains()
        val test = hashMapOf<String, Int>()

        test.get

        answer = temp.sorted().toIntArray()
        return answer
    }
}